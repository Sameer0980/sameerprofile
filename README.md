# Sameer Portfolio

Static GitHub Pages version.

## Files

Upload these files to the root of your repository:

- `index.html`
- `.nojekyll`
- `README.md`

Also upload your portrait SVG as:

- `file.svg`

The page expects the portrait at `file.svg` in the repository root.
